# 06 - 项目执行清单（P0～P5，可打勾）

> 用法：在编辑器里把 `- [ ]` 改成 `- [x]` 表示已完成；或直接在支持任务列表的预览里勾选。  
> 详细需求见 [01-功能性需求](01-requirements-functional.md)、硬件见 [03-硬件设计](03-hardware-design.md)。  
> **阶段性开发目标与验收表（从当前固件到产品形态）**见 [07-开发路线图](07-development-roadmap.md)。

---

## 里程碑速览

| 代号 | 名称 | 完成标志（一句话） |
|------|------|---------------------|
| **P0** | 原型验证 | 开发板 + EC800 **数传/GNSS 板**：三家卡 **PDP 数据 + GNSS** 在你所在地可用（**不强制 VoLTE**） |
| **P1** | 固件原型 | 圆屏上 LVGL 表盘稳定；UART AT 驱动 **PDP/MQTT** + GNSS；MQTT/HTTPS 能连自签后端 |
| **P2** | 后端 + 家长 APP | 云上单机 Docker 跑通；APP 登录、绑定、地图、基础 API |
| **P3** | 端到端联调 | 手表（开发板形态）上报位置、微聊、围栏、SOS 链路打通 |
| **P4** | 自研 PCB | KiCad 原理图+PCB+SMT 样机；固件移植到新板 |
| **P5** | 结构 + 验收 | 表壳迭代；72h 老化、7 天佩戴、续航/防水达标 |

---

## P0 原型验证（采购 + 接线 + 运营商）

**目标**：不画 PCB，先证明「EC800 系列模组 + 你办的卡 + 当地网络」能支撑 **数据 + GNSS**（V1 语音走 APP，见 [00-愿景 §4.0](00-vision.md)）。

### P0.1 采购与到货

- [x] 下单 ESP32-S3 圆屏开发板（1.43" AMOLED，与文档选型一致或等价）× **2**
- [x] 下单移远 **EC800 系列** 开发件 × **2**（**数传核心板可**，须带 **LTE+GNSS 天线位**；含天线、文档或规格书）
- [x] 办理 / 激活 **3 张**不同运营商 Nano SIM（移动、电信、联通各一，儿童副卡或主卡副卡）
- [ ] 备齐杜邦线、USB 线、万用表、可选 USB 电流表
- [ ] 到货清点：板子版本、天线、排针是否焊接齐全

### P0.2 文档与工具

- [x] 电脑安装 **ESP-IDF v5.2+**（与 [04-固件架构](04-firmware-architecture.md) 一致；**或 v6.x 稳定版**，见官方安装器）
- [ ] **4G 板 USB 转串口驱动**：设备管理器中出现 **COM 口**（无黄色感叹号）
- [ ] **串口调试软件已装并会连**：PuTTY / MobaXterm / 友善串口助手 等任一；能选 **COM + 波特率（多为 115200）** 打开会话
- [ ] **与模组 AT 会话稳定**：手动发 `AT` 有 **`OK`**；能间歇看到 **`+` 开头的 URC**；连续 **5 分钟**不乱码、不断线
- [ ] 阅读所购模组硬件手册中与 **UART 电平、PWRKEY、SIM、PDP** 相关章节（EC800N/K/M 以实际料号为准）
- [ ] 建立一页「原型记录」（可本文件末尾加「记录」小节，或单独 `hardware/docs/p0-log.md`）

> **说明**：看 **ESP32 圆屏板** 的 log 时，用 **ESP-IDF 自带** 的 `idf.py monitor`（接板子 USB）；看 **4G 模组** 的 AT/URC 时，一般是 **另一根 USB 接 4G 板** 用 PuTTY 等打开 **第二个 COM 口**。两块板都插电脑时，会各占一个 COM。

### P0.3 电气连接（杜邦线原型）

- [ ] 确认开发板与评估板 **GND 共地**
- [ ] UART：ESP32 TX → 模组 RX、RX → TX，**3.3V 电平**（禁止 5V）
- [ ] 评估板独立供电或按手册供电，避免 USB 口电流不足导致掉网
- [ ] SIM 卡插入正确、接触良好；三张卡分别贴标签（移/电/联）

### P0.4 AT 与网络基线

- [ ] 模组上电后能响应 `AT`，回 `OK`
- [ ] `AT+CPIN?` 显示已就绪（非 SIM PIN 锁死状态）
- [ ] 三张卡分别完成：**注册成功**（CREG/CEREG 或模组等效指示）
- [ ] 记录每张卡的 **IMEI、ICCID、信号强度**（便于以后对照）

### P0.5 蜂窝数据与「APP 语音」链路（核心风险项）

> V1 **不强制 pstn 电话**。本节约验证：**能稳定上网** → 手表经 TLS 连自家服务（可先 `curl`/MQTT 测试）→ 为 **Opus 语音上传** 铺路。

- [ ] **移动卡**：PDP 激活成功，能 **TCP/TLS** 访问公网或你的服务器（记录 RTT）
- [ ] **移动卡**：MQTT over TLS **连接 + 订阅 + 发布** 各测 ≥10 次成功
- [ ] **电信卡**：同上
- [ ] **联通卡**：同上
- [ ] （可选加分项）若仍想验证语音模组能力：再测 **VoLTE 拨号**；非 V1 必选项
- [ ] 若某运营商数据失败：保存完整 **AT 日志 + 城市 + 卡套餐名称**，列入风险单

### P0.6 GNSS 定位

- [ ] 户外开阔地：冷启动到 **首次定位**时间记录（目标参考 [00-愿景](00-vision.md) 成功指标）
- [ ] 解析经纬度（NMEA 或 `AT+QGPSLOC` 等模组指令，以你评估板文档为准）
- [ ] 室内/窗边：记录「无 fix / LBS 回退」行为，供后端融合策略参考

### P0.7 功耗与热（粗测 + 子系统功耗基线）

> 目的：整机续航在 **P5** 才精测；**P0** 须建立 **各子系统电流/发热数量级**，便于发现异常漏电、模组未休眠、GNSS 常开等问题。与 [02 - NFR-PWR](02-requirements-nonfunctional.md)「功耗测量阶段约定」一致。

**最低要求（仍须勾选）**

- [ ] 4G **驻网待机** 电流有大致范围（心里有数 / 或记下 mA）
- [ ] **传数据**（MQTT 一小段或 TCP 脉冲）时模组 **表面温度** 手触可接受（烫手则 P4 布局须加重散热）

**子系统基线（建议逐项记录到 `hardware/docs/p0-log.md` 或表格）**

测量方式：**USB 供电粗测**或 **万用表串入 5V 供电线** 估整机/板总电流；备注供电方式（单 4G 板 / 与 ESP 同供）。

| 子系统 | 建议工况 | 记录内容 |
|--------|----------|----------|
| **屏幕** | 亮屏最高亮度 vs 息屏/关背光（若可调） | 相对电流档位或 mA；是否明显发热 |
| **4G** | 仅注册待机 vs PDP 已激活空闲 vs 传数据 30s | 三档电流/体感；模组壳温 |
| **GNSS** | 冷启动搜星前 2min vs 已定位维持 | 电流峰值、是否显著烫手；无 GNSS 料号则写「跳过 / 仅 LBS」 |
| **录音** | I2S 采集（麦克风接到后补测；P0 可跳过） | 相对基线增量 |

- [ ] 上表至少 **屏幕、4G、GNSS** 三行有记录（录音可标 N/A）
- [ ] （可选）有 **万用表电流档** 时，在备注中写 **粗 mA**，便于与 NFR 预算对照

### P0.8 P0 收口判定（全勾才算 P0 完成）

- [ ] **至少 2/3 家运营商** PDP + MQTT/TLS 业务成功（与 [00-愿景 §6](00-vision.md) 成功指标一致）
- [ ] GNSS 在常用场景下 **可用且有记录**（若所购料号无 GNSS，本项改为「明确记录仅 LBS」并回退选型）
- [ ] 已写一页结论：**推荐主力运营商 + 备用卡（流量套餐）**，供 P4 BOM 与资费不再摇摆

---

## P1 固件原型（开发板上的软件）

> **与 P0 并行**：硬件在途时，可先完成 **§P1.0**（仅电脑、不接板也可 `build`）；**LVGL / 触摸 / 麦克风 / 接 4G UART** 等须等圆屏板与蜂窝板到货后继续。

### P1.0 无硬件可并行（编译链）

- [ ] 本机 ESP-IDF 已安装，`idf.py --version` 正常
- [ ] 在仓库 `firmware/` 目录执行 `idf.py set-target esp32s3` 与 `idf.py build` **无报错**（可不接 USB，见 [firmware/README.md](../firmware/README.md)）

**固件目录与命令（与 [07-开发路线图](07-development-roadmap.md) 阶段 0.4 一致，便于检索）**

- **工程根目录（须 `cd` 到此再执行 `idf.py`）**：仓库内 **`watch-diy/firmware/`**（完整路径示例：`d:\repository\watch-diy\firmware`）。
- **编译**：`idf.py build` → 产物如 **`firmware/build/watch_diy_fw.bin`**（以 `CMakeLists.txt` 中 `project()` 名为准）。
- **烧录**：`idf.py -p COMx flash`（`COMx` 换成本机端口）。
- **监视**：`idf.py -p COMx monitor`；或一条命令 **`idf.py -p COMx flash monitor`**。
- **分区表（与 07 阶段 0.3 一致）**：`firmware/partitions_watch.csv`，Kconfig 中 **`CONFIG_PARTITION_TABLE_CUSTOM_FILENAME`** 指向该文件；用途与 OTA 占位说明见 [firmware/README.md](../firmware/README.md) 内「分区表（阶段 0.3）」一节。

### P1.1 工程与显示

- [x] 仓库内已初始化 **`firmware/`** ESP-IDF 工程骨架（`CMakeLists.txt`、`main/`、`sdkconfig.defaults`，说明见 `firmware/README.md`）
- [ ] 集成 **LVGL 9.x**，圆屏 466×466 显示正常（无撕裂可后调 TE）
- [ ] 表盘页：时间、电量（可先假数据）、信号图标占位
- [x] 触摸：三屏 **`lv_scr_load` 切换**（表盘 / 设置列表 / 关于），无重复 `create` 泄漏路径（见 `ui_demo.c`）

### P1.2 EC800 驱动骨架（N/K/M 以实际料号为准）

- [ ] UART 驱动：波特率、流控与底板一致
- [ ] **AT 队列**：一条一条发，超时与重试策略可跑
- [ ] 封装：注册状态查询、**PDP 激活/去激活**、TCP/TLS 或模组内置 HTTP（二选一先跑通）、短信（可选）
- [ ] （可选）**pstn 拨号/挂断**：仅当模组与 SIM 支持且你打算做 V1.x 电话时实现
- [ ] GNSS：开/关、读一帧位置，喂给简单 UI 或串口 log（模组无时则跳过并文档化）

### P1.3 网络业务雏形

- [ ] PDP 激活后能 **HTTPS GET** 到你服务器某路径（或先 `https://httpbin.org/get` 验证 TLS 栈）
- [ ] 嵌入 **自签 CA**（见 [05-后端与 APP](05-cloud-and-app.md)），校验链正确
- [ ] **MQTT over TLS** 连上 EMQX（可先只订阅 `watch/test/#`）

### P1.4 音频（网络语音 / 微聊路径）

- [ ] 麦克风 + 扬声器走 **ESP32 I2S**（与 [04-固件架构](04-firmware-architecture.md) 微聊路径一致）
- [ ] **Opus 编码** 小段语音 → 经 PDP **HTTPS POST** 或 **MQTT 二进制负载** 上传（先通一种）
- [ ] APP 或后端测试页能 **播放** 收到的语音（证明闭环）

### P1.5 P1 收口判定

- [ ] 表盘 + 基础导航 **30 FPS 可接受**（主观 + LVGL FPS 计数）
- [ ] AT 驱动能支撑：**PDP + MQTT + GNSS**（及可选 pstn）同一套工程里切换不崩溃
- [ ] 自签 TLS 在设备上 **无证书错误**

### P1.6 功耗建模与低功耗（与 [02 - NFR-PWR-001 / 006](02-requirements-nonfunctional.md) 对齐）

> **P1 末 / P2 初**至少要有 **可开可关、能测电流差** 的实验；不要求手表已达 24h 续航。

- [ ] **24h 纸面 mAh**：用 P0.7 粗测电流 + 计划占空比（MQTT 间隔、GNSS 频次等）做 **一页表格**，验证 **500mAh 量级电池** 与 **NFR-PWR-001** 不自相矛盾（存 `hardware/docs/power-model.md` 或表格）
- [ ] **ESP32-S3**：至少一种 **Light sleep**（或 Deep sleep）路径可被定时器/GPIO 唤醒，且实测 **休眠前后电流差**
- [ ] **蜂窝模组**：阅读 AT 手册中 **PSM / eDRX**（若支持）；在开发板上做 **开关各一轮**，记录对 **驻网、MQTT 下行时延** 的影响
- [ ] 在 [04-固件架构](04-firmware-architecture.md) 或 `firmware/` 内文档 **记下结论**：哪些策略 **进 V1**，哪些 **延到 P4**

### P1.7 内存与并发（与 [04 - §3.2 / §3.5](04-firmware-architecture.md) 对齐）

- [ ] 代码或配置中落实 **模块 PSRAM 上限**（LVGL / AT / Opus / TLS 等），避免匿名大块泄漏
- [ ] **压测**：表盘 + MQTT +（可选）注入长 URC 或模拟 RX + Opus 编码，**≥10 分钟** 无堆分配失败；`min free PSRAM` 不低于 **04 §3.5.3** 约定阈值（无则先记最小值日志备查）
- [ ] 实现或文档化 **`resource_arbiter` 优先级**：至少覆盖 **录音上传进行时，GNSS 定时采集让路**（与 04 默认策略一致）

---

## P2 后端 + 家长 APP（可并行）

**目标**：云上单机 Docker 全栈；APP 能登录、绑设备、看地图、调部分 API。

### P2.1 服务器与 TLS

- [x] 云主机 `/opt/watch/` 目录与 `docker-compose` 骨架就位
- [x] 生成 **自签 CA + 服务器证书（SAN=公网 IP）**，Caddy `:18443` 可访问
- [x] 安全组放行：`18443`、`18883`、`19000`（及 SSH 仅你家 IP，若可配）
- [x] 浏览器或 `curl -k` 验证证书链（理解 `-k` 与正式客户端 pinning 区别）

### P2.2 数据与中间件

- [ ] PostgreSQL 初始化表结构（参考 [05-后端与 APP](05-cloud-and-app.md) §3）
- [ ] Redis、MinIO 桶与策略（录音/照片私有读）
- [ ] EMQX 启动，TLS  listener `18883`，控制台仅本机

### P2.3 Go API MVP

- [ ] 项目脚手架：配置、日志、健康检查 `/health`
- [ ] 短信登录：`/auth/sms-code`、`/auth/login`（可先 mock 验证码 dev 模式）
- [ ] 设备绑定：`POST /devices/bind` 与 JWT 签发
- [ ] MQTT HTTP 鉴权钩子（设备连接校验）

### P2.4 Flutter APP MVP

- [ ] 工程创建，**ca.pem** 嵌入，Dio pinning 可访问 `https://IP:18443`
- [ ] 登录页 → 首页占位
- [ ] 高德（或先静态地图）显示 **最近一次位置**（可先手工 POST 一条）
- [ ] 扫码绑定流程 UI（可与假设备联调）

### P2.5 P2 收口判定

- [ ] 手机 4G/WiFi 下 APP 能稳定连自家 API + MQTT（各测 10 次）
- [ ] 一条完整数据：**用户登录 → 绑定设备 ID → 数据库有记录**

---

## P3 端到端联调（功能闭环）

**目标**：仍以开发板 + 评估板形态，跑通「家长视角」主路径。

### P3.1 定位与轨迹

- [ ] 手表侧定时上报位置（MQTT 或 HTTP，与架构一致）
- [ ] APP 地图实时刷新 + 历史轨迹查询（至少当天）

### P3.2 语音联系与白名单（APP 侧）

- [ ] 后端/设备侧维护 **可发语音的家庭成员** 白名单（可先写死配置）
- [ ] 非成员设备/用户拒绝订阅或拒绝下行（最小安全）

### P3.3 微聊

- [ ] 手表录音上传 MinIO，元数据进 PG
- [ ] MQTT 推送到 APP；APP 播放 Opus 或转码后播放

### P3.4 电子围栏与 SOS

- [ ] 围栏配置从 APP 下发到设备（MQTT sync）
- [ ] 进出围栏事件 → 推送通道到手机（先做一家厂商 + iOS）
- [ ] SOS：长按逻辑 + **强提醒链路**（推送 + 位置）+ 可选短信；**pstn 拨号**为可选项

### P3.5 P3 收口判定

- [ ] 家长一天内可完成：**看位置、收微聊、收围栏告警、触发 SOS 能收到通知**（允许 UI 粗糙）

---

## P4 自研硬件（KiCad PCB + SMT + 移植）

**前提**：P0 结论稳定，P1～P3 无「必须改模组」的颠覆性问题。

### P4.1 原理图

- [ ] KiCad 工程：`hardware/schematic/` 目录规范见 [03-硬件设计](03-hardware-design.md) §11
- [ ] 电源树：SY6970、电池、EC800 **VBAT/供电轨**、3.3V 轨、去耦电容等级完整
- [ ] ESP32-S3 + EC800：UART、（可选）PCM、PWRKEY、复位、状态脚
- [ ] 屏、触摸、IMU、RTC、电量计、音频、摄像头、SIM 座、天线电路
- [ ] 设计评审：至少自己对照 checklist 过一遍，能请朋友 review 更佳

### P4.2 PCB

- [ ] **4 层板**叠层与阻抗说明（见 [03-硬件设计](03-hardware-design.md) §5）
- [ ] DRC 无错、制造说明（板厚、沉金、阻抗测试条若需要）
- [ ] 导出 **Gerber + 钻孔 + BOM + 坐标（Pick&Place）**
- [ ] 嘉立创（或其他）下单 **SMT**：料齐、替代料规则写好

### P4.3 打样与上电

- [ ] 首板上电：电源轨电压、无短路冒烟
- [ ] ESP32 能烧录、串口 log 正常
- [ ] EC800 能找网、SIM 识别
- [ ] 屏、触摸、GNSS、音频逐项 bring-up（记录每步问题）

### P4.4 固件移植

- [ ] `sdkconfig` 与引脚与原理图 **GPIO map** 完全一致
- [ ] 功能对照 P3：**逐项**在自研板上回归
- [ ] OTA 分区表与 bootloader 在量产样机上验证一次

### P4.5 P4 收口判定

- [ ] **至少 1 块**自研板功能与开发板原型等价（允许已知小问题 list）
- [ ] BOM+坐标已归档，可重复下单第二板

---

## P5 结构、可靠性、验收

**目标**：能给孩子戴出门；满足 [00-愿景](00-vision.md) / [02-非功能](02-requirements-nonfunctional.md) 里 V1 成功指标。

### P5.1 表壳与表带

- [ ] Fusion 360（或其他）表壳第一版 STL
- [ ] SLA 打印 → 装配 → 修模，**至少 3 版迭代**记录
- [ ] 表带 18 mm、防拆螺丝、SIM 防拔结构实装验证

### P5.2 环境与安全测试

- [ ] **72h** 老化（充电循环 + MQTT 心跳 + 偶发语音上传脚本）
- [ ] **7 天**真实佩戴（大人先戴一周再给孩子）
- [ ] 续航：按 [02-NFR-PWR-001](02-requirements-nonfunctional.md) 典型场景测满一次
- [ ] IPX5 喷淋（或等效）与麦克风/充电部位检查进水

### P5.3 文档与交付

- [ ] 更新 `docs/03-hardware-design.md` 中「与实物一致」的 BOM 与装配注意
- [ ] 已知问题列表（issue 或 `docs/known-issues.md`）
- [ ] 备份：服务器 nightly、CA 私钥离线备份复查

### P5.4 P5 / V1 收口判定

- [ ] [00-愿景](00-vision.md) §6 成功指标表中 **V1 项**自检通过（或明确标注例外与原因）
- [ ] 4 台样机状态表：每台 SN、固件版本、主用运营商

---

## 并行工作提示（甘特图里重叠的部分）

- [ ] **P4 画板** 可与 **P3 联调** 部分并行，但不要早于 **P0 PDP+GNSS 结论** 定稿
- [ ] **P5 表壳** 可在 **P4 PCB 发板后** 就开始打印第一版，利用等待快递的空窗

---

## 完成度统计（可选）

在阶段结束时更新：

| 阶段 | 总项（约） | 已完成 | 完成率 |
|------|------------|--------|--------|
| P0 | 自数本清单 | | |
| P1 | | | |
| P2 | | | |
| P3 | | | |
| P4 | | | |
| P5 | | | |

---

**上一篇**：[05 - 后端与 APP 架构](05-cloud-and-app.md)  
**回到索引**：[README](../README.md)
